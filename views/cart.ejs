<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Bohao's Supermarket</title>
</head>

<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Bohao's Supermarket</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/addProduct">Add new product</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container">
    <br>
    <div class="text-center">
      <h2>Shopping Cart</h2>
    </div>
    <br>

    <table class="table table-hover small text-center">
      <thead>
        <tr>
          <th width="50">Product Name</th>
          <th width="100">Product Image</th>
          <th width="50">Product Quantity</th>
          <th width="50">Product Price</th>
          <th width="100">Description</th>
          <th width="50">Edit</th>
          <th width="50">Delete</th>
        </tr>
      </thead>
      <tbody>
        <% for(let i=0; i < product.length; i++) { %>
          <tr>
            <td>
              <a href="/product/<%= product[i].productId %>">
                <%= product[i].productName %>
              </a>
            </td>
            <td>
              <img src="/images/<%= product[i].productImage %>" width="40%">
            </td>
            <td>
              <%= product[i].productQuantity %>
            </td>
            <td>$<%= product[i].productPrice %>
            </td>
            <td>
              <%= product[i].productDescription %>
            </td>
            <td><a href="/editProduct/<%= product[i].productId %>">Edit</a></td>
            <td><a href="/deleteProduct/<%= product[i].productId %>"
                onclick="return confirm('Are you sure you want to delete this product?')">Delete</a></td>
          </tr>
          <% } %>
      </tbody>
    </table>
  </div>
</body>

</html>